# Mobile-write smoke test

This file confirms `update_file(repo="a-pap/memex", ...)` works after PR #245 added memex to ALLOWED_WRITE_REPOS in the claude-memory-mcp worker.

Written 2026-05-01 from Claude Code CLI through the MCP endpoint as if it were a mobile claude.ai session.
