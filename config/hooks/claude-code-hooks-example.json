{
  "_comment": "Claude Code SessionEnd hook — calls auto_log MCP on session close. Paste the hooks block into ~/.claude/settings.json or .claude/settings.local.json (merge with existing). Script path is the one committed in this repo.",
  "hooks": {
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash /Users/<user>/GitHub/claude-memory/config/hooks/auto-log-session.sh"
          }
        ]
      }
    ]
  }
}
